[package]
name = "cli"
version = "0.1.0"
edition = "2024"

[dependencies]

common = { path = "../common"}
broker = { path = "../broker" }
producer = { path = "../producer" }
consumer = { path = "../consumer" }
dirs = "6.0.0"
tokio = { version = "1.53.1", features = ["full"] }
